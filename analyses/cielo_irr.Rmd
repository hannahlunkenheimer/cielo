---
title: "cielo1_irr"
author: "Hannah Lunkenheimer"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(psych)
library(irr)
library(readr)
library(reshape2)

d_paola <- readr::read_csv("~/Documents/GitHub/cielo/data/paolacoding_irr_cielo copy.csv")
d_helen <- readr::read_csv("~/Documents/GitHub/cielo/data/helencoding_irr_cielo copy.csv")

```

```{r irr}
d_paola <- melt(d_paola, "V1")
d_helen <- melt(d_helen, "V1")

total <- merge(d_paola, d_helen,by="V1")

#for (dimension in 1:78) {
#    v = paste0("V", dimension)
#    print(irr::kappa2(cbind(d_helen[, v], d_paola[, v])))
#}

#overall IRR is .92
kappa2(total[, c("value.x", "value.y")], weight = "unweighted")

```
